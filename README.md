# symmetrical-companion

This is a signalling server that creates a global hub for connected websocket clients. 

When a client sends a message, the server broadcasts the message to everyone except the sender.
